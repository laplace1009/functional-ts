{
  "compilerOptions": {
    "target": "ES6",           // ES5 호환성을 위해
    "module": "ES6",      // Node.js 환경을 대상으로 할 경우
    "lib": ["ES6", "DOM"],     // 컴파일 시 사용할 내장 API 선언
    "declaration": true,       // .d.ts 파일 생성
    "outDir": "./lib",         // 출력 디렉토리
    "strict": true,            // 엄격한 타입 검사 활성화
    "esModuleInterop": true,
    "moduleResolution": "Node"// ES 모듈과 CommonJS 모듈 간 호환성 향상
  },
  "include": [
    "src/**/*"                // 컴파일할 파일이 위치한 디렉토리
  ],
  "exclude": [
    "node_modules",           // 제외할 디렉토리
    "**/*.test.ts"            // 테스트 파일 제외
  ]
}
